#include "part3.h"
#include <iostream>
#include <string.h>
#include <assert.h>
using namespace std;

int *count;
Stringset C;


const int N = 10;
int a[N] = {7, 3, 3, 1, 3, 5, 3, 2, 5, 2};

int main() {
  
  int tempcount;
  for(int i = 0; i < N; ++i) {
    C.insert(a[i]);
  }

  for(int i = 0; i < size; ++i) {
    for(Node* n = table[i]; n != NULL; n = n->next) {

    }
  }
}
